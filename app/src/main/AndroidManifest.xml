<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="sk.tuke.tictactoe">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TicTacToe">

        <!-- LoginActivity - now set as the launch (start) activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- RegisterActivity for creating a new account -->
        <activity
            android:name=".RegisterActivity"
            android:exported="true" />

        <!-- WelcomeActivity - no longer the launch activity -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="true" />

        <!-- TicTacToe (Main) Activity -->
        <activity
            android:name=".TicTacToeActivity"
            android:exported="true" />

        <!-- GameListActivity declaration -->
        <activity
            android:name=".GameListActivity"
            android:exported="true" />

        <!-- GameDetailActivity declaration -->
        <activity
            android:name=".GameDetailActivity"
            android:exported="true" />

        <!-- Game2048Activity declaration -->
        <activity
            android:name=".Game2048Activity"
            android:exported="false" />

        <activity
            android:name=".LeaderboardActivity"
            android:exported="true" />

    </application>
</manifest>